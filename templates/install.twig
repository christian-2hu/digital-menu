<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirmação de Instalação do Banco de Dados</title>
    <!-- Inclusão do Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <!-- Caixa de alerta com informações importantes -->
        <div class="alert alert-warning" role="alert">
            <h4 class="alert-heading">Atenção!</h4>
            <p>Você está prestes a instalar o banco de dados. Se fizer isso e já existir uma instância, todos os dados serão deletados. Só prossiga se souber exatamente o que está fazendo.</p>
            <hr>
            <p class="mb-0">Deseja prosseguir com a instalação?</p>
            <p>{{test}}</p>
        </div>
        
        <!-- Botões de confirmação -->
        <div class="d-flex justify-content-end">
            <!-- Botão para cancelar -->
            <!-- Botão para confirmar -->
            <div class="form">
                <form method="POST" action="install.php">
                    <button type="submit" class="btn btn-danger">Sim, instalar banco de dados</a>
                </form>
            </div>

        </div>
    </div>

    <!-- Inclusão do Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>